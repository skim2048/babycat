# Debugging Web UI

디버그용 웹 대시보드 (Vue 3 + Vite).

## 실행 방법

**반드시 이 디렉토리에서 실행해야 합니다.**

```bash
cd debugging
docker compose up -d
```

메인 스택(`docker compose up -d`)이 먼저 실행된 상태여야 합니다.

## 종료

```bash
cd debugging
docker compose down
```
