```mermaid
flowchart TB
    User(("User"))
    ClientApp["Client app"]
    VideoSource["Video source"]

    subgraph Babycat
        Router["router"]
        Streamer["streamer"]
        Analyzer["analyzer"]
        Recorder["recorder"]
    end

    User --> ClientApp
    ClientApp -->|"HTTP: 8000"| Router
    ClientApp <-.->|"WebRTC Media/ICE: 8890/udp"| Streamer

    Router -->|"프로필·PTZ·활성화, HLS·WHEP 중계"| Streamer
    Router --> Analyzer
    Router --> Recorder

    Streamer <-->|"RTSP"| VideoSource
    Streamer -->|"ONVIF"| VideoSource
    Streamer <-->|"RTSP: 8554"| Analyzer
    Streamer <-->|"RTSP: 8554"| Recorder

    Analyzer -->|"이벤트 통지"| Recorder
```
