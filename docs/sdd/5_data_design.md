# 5. 데이터 설계 (Data Design)

## 5.1 데이터 모델 (Data Model)

<!-- 시스템이 다루는 데이터의 종류와 그들 사이의 관계를 적는다. SRS §6.4가 정한 논리적 범위를 물리적 모델로 구체화하는 자리다. -->

## 5.2 데이터베이스 스키마 (Database Schema)

<!-- 테이블, 컬럼, 자료형, 제약, 인덱스를 적는다. 조회 조건(키워드, 날짜)이 인덱스 설계의 근거이므로 함께 밝힌다. -->

## 5.3 파일시스템 구조 (Filesystem Layout)

<!-- 비디오 클립과 사이드카 메타데이터를 어떤 경로 규칙으로 배치하며, 데이터베이스의 레코드와 어떻게 연결할지 적는다. -->

## 5.4 설정 데이터 (Configuration Data)

<!-- 비디오 소스 프로필, 프롬프트, 이벤트 키워드 등 사용자가 설정하는 값의 저장 형식과 기본값을 적는다. 외부에서 주입받는 값은 §8.3에서 다룬다. -->

## 5.5 데이터 수명 주기 (Data Lifecycle)

<!-- 데이터가 생성되어 삭제되기까지의 경로를 적는다. 저장 공간이 임계치 이하로 떨어졌을 때의 자동 삭제(FR-033) 판단 기준과 삭제 순서가 핵심이다. -->
