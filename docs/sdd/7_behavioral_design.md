# 7. 동작 설계 (Behavioral Design)

## 7.1 주요 시나리오 흐름 (Primary Scenario Flows)

<!-- SRS §2.3의 일곱 시나리오를 컴포넌트 간 호출 수준으로 상세화한다. SRS가 서술식으로 남긴 단계를 시퀀스 다이어그램으로 옮기는 자리다. -->

## 7.2 장면 분석 파이프라인 (Scene Analysis Pipeline)

<!-- 프레임 추출부터 VLM 입력, 텍스트 생성, 키워드 매칭, 클립 저장까지의 처리 흐름을 적는다. 프레임 추출 주기와 추론 지연의 관계가 핵심 논점이다. -->

## 7.3 상태 전이 (State Transitions)

<!-- 분석의 정지와 구동, 스트림 연결 상태 등 시스템이 가지는 상태와 그 전이 조건을 적는다. 어떤 요청이 어떤 상태에서만 유효한지도 함께 밝힌다. -->

## 7.4 동시성과 자원 경합 (Concurrency and Resource Contention)

<!-- 동시에 일어날 수 있는 작업이 무엇이며, 16GB 메모리와 하드웨어 가속기를 어떻게 나눠 쓸지 적는다. VLM 모델 전환 시의 메모리 반환과 재적재(FR-032, NFR-023)가 여기에 속한다. -->

## 7.5 오류 처리와 복구 (Error Handling and Recovery)

<!-- 스트림 단절, 추론 실패, 저장 실패 등 예상되는 실패를 열거하고 각각의 대응을 정한다. 재시도 정책과 컨테이너 재시작에 맡길 범위를 구분한다. -->
