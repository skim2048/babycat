# @claude Babycat — web dashboard container (dev).
# @claude Vue 3 + Vite dev server.
# @claude node_modules is populated under the bind-mounted project root (./node_modules).

FROM node:22-slim

WORKDIR /app

EXPOSE 5173
CMD ["sh", "-c", "npm install && npm run dev"]
