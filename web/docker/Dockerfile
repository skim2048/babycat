# Babycat — Web Dashboard Container (dev)
# Vue 3 + Vite dev server
# node_modules는 bind mount된 프로젝트 루트(./node_modules)에 생성됨

FROM node:22-slim

WORKDIR /app

EXPOSE 5173
CMD ["sh", "-c", "npm install && npm run dev"]
